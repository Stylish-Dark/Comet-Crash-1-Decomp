# Generated analysis

Run `tools/m1_analyze.py` against the user's local decrypted NPEB00142 v1.00 ELF.

Tracked outputs may include loader metadata, import inventories and compatibility reports. The executable itself and extracted SPU ELF binaries must remain under `local/` or another ignored path.

Before committing newly generated metadata, inspect it for accidental embedded binary blobs or proprietary asset content.
